
	<h2 id="masthead-subtitle">
		<ul>
			Watch someone's dog. <br/>
			Earn swaps. <br/>
			Get free dog sitting from other owners. 
		</ul>
	</h2>

	<% if current_user %>
      <a class= "button signupbutton">Welcome <strong><%= current_user.name %></strong>!</a>
      <%= form_for @user, :html => { :multipart => true } do |form| %>
  	    <%= form.file_field :avatar %>
      <% end %>
    <% else %>
	  <a href="/auth/facebook" id="sign_in" class="button signupbutton">Sign in with Facebook</a>
	<% end %>
<div id="hellolinks">
	<h4><%=link_to "Can I pay Cash instead? (yes)", statics_url%></h4> 
</div>